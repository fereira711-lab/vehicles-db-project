Rode o script de build do banco de veículos FIPE.

Se o argumento --check foi passado, apenas verifique se o banco existe e mostre as estatísticas.

Caso contrário, execute o build completo:
```bash
node build-vehicles-db.js $ARGUMENTS
```

Mostre o progresso e resultado final ao usuário.
